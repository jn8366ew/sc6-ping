# 이 코드가 어떻게 도는가

`monitor.py` 한 파일이 실제로 **어떤 순서로 실행되는지**를 위에서 아래로 따라간
문서다. 각 상수의 근거는 [FINDINGS.md](FINDINGS.md)가, 왜 그걸 재야 하는지는
[NETWORK.md](NETWORK.md)가 갖고 있다.

| 문서 | 답하는 질문 |
|---|---|
| **ARCHITECTURE.md** (여기) | **어떻게 도는가** — 실행 흐름, 스레드, 데이터 경로 |
| [README.md](README.md) | 이게 뭔가 — 프로젝트 소개와 측정 설계 판단 |
| [FINDINGS.md](FINDINGS.md) | 왜 그 값인가 — 상수의 실측 근거와 변경 이력 |
| [NETWORK.md](NETWORK.md) | 왜 이런 게 문제인가 — 네트워크 개념 |
| [USAGE.md](USAGE.md) | 어떻게 쓰나 — 실행, 출력 필드, 트러블슈팅 |

줄번호는 `함수명 줄번호` 관례로 붙인다 — 코드가 움직여도 심볼명으로 `grep`하면 찾을 수 있다.

---

## 읽는 순서

**개념부터 읽지 않는다.** 진입점에서 아래로 내려가는 게 빠르다.

| # | 무엇 | 위치 | 왜 여기부터 |
|---|---|---|---|
| 1 | 파일 상단 독스트링 | 1~31 | 4줄짜리 동작 원리와 딜레이 넷코드 전제 |
| 2 | `run()` | 1827 | 전체 생애 — GeoDB 비동기 로드 → Pinger 기동 → 세션 루프 |
| 3 | `monitor_session()` | 1664 | **이 프로그램의 본체.** 150줄짜리 메인 루프 |
| 4 | 수동 측정 경로 | 1687 → 641 → 655 → 568 | `on_packet` → `TrafficWindow` → `Flow` |
| 5 | 능동 측정 경로 | 890 → 705 → 780 → 1021 | `Pinger.run` → `ping_once` → 폴백 → `snapshot` |
| 6 | 상대 선정 경로 | 188 → 202 → 220 → 257 → 1267 | 프로세스 → 포트 → `PortSet` → BPF → `pick_opponent` |
| 7 | 출력 | 1295 / 1325 / 1040 | `format_osd` / `format_log` / `OSD` |
| 8 | 판 상태기계 | 1468 → 1420 → 1571 → 1612 | `MatchTracker` → `MatchSummary` → 요약 → `SessionTally` |
| 9 | 곁가지 | 431 / 322 / 347 / 1081 | 본 흐름과 독립. **나중에** 읽는다 (7절) |

3번까지 읽으면 골격이 잡힌다. 4~6번이 그 골격에 매달린 세 갈래다.

---

## 1. 세 개의 루프와 공유 상태

```
[캡처 스레드]  AsyncSniffer(prn=on_packet)  ─ 연속
      │   on_packet 1687 — pkt.time(Npcap 드라이버 타임스탬프)만 쓴다
      ▼   ┌───────────── TrafficWindow._lock 630 ─────────────┐
   TrafficWindow 621   _inbound / _outbound / _relay : IP → deque[시각]
      ▲   └───────────────────────────────────────────────────┘
      │   snapshot() 655 — 최근 2초로 잘라 IP별 Flow 생성
      │
[메인 스레드]  monitor_session 1664 ─ 250ms 틱
      │   ├ 5초  : 포트 재열거 → BPF 갱신 → pick_opponent → pinger.set_target
      │   ├ 250ms: osd.show(format_osd)
      │   └ 2초  : print(format_log) + tracker.feed   ← 판 표본은 여기서만
      ▲   ┌───────────── Pinger._lock 855 ─────────────┐
      │   pinger.snapshot() 1021 → {avg, lo, hi, loss, samples, hop}
      │   └────────────────────────────────────────────┘
[핑 스레드]   Pinger.run 890 ─ 1초
      └ [추적 스레드] _trace_worker 952 ─ 상대당 1회, 최대 14초
```

### 스레드 경계는 딱 두 곳이다

`TrafficWindow`(캡처↔메인)와 `Pinger`(핑↔메인). **그 밖의 상태는 전부
`monitor_session()`의 지역 변수라 락이 없다** — `opponent`, `opp_geo`,
`port_set`, `probes`, `tracker`, `tally`는 메인 스레드만 만진다.

두 경계 모두 같은 모양이다. 생산자가 락 안에서 `append`하고, 소비자가 락 안에서
**스냅샷 객체를 만들어 나온다**(`Flow`, `dict`). 소비자가 공유 자료구조를 들고
나가지 않으므로 표시 코드에는 락이 아예 없다.

### 틱 세 개가 서로 다른 이유

| 틱 | 주기 | 근거 |
|---|---|---|
| 측정 창 | 2.0초 (`JITTER_WINDOW` 60) | 130샘플 ≈ 통계를 낼 만큼 |
| 표시 | 250ms (`DISPLAY_INTERVAL` 64) | 초당 4번 넘게 바뀌면 사람이 못 읽는다 |
| 로그 / 판 표본 | 2.0초 (`LOG_INTERVAL` 65) | **측정 창과 같아야 한다** |

마지막 줄이 함정이다. `tracker.feed()`를 250ms 표시 틱에서 부르면 2초짜리 창이
8겹으로 겹쳐 **같은 패킷을 8번 세게 된다.** 그래서 판 표본은 로그 틱에서만
모은다 (`MatchTracker` 독스트링 1471, 호출부 1779).

### 추적 스레드가 따로 있는 이유

`Pinger.run` 안에서 `tracert`를 직접 돌리면 그동안 핑이 한 발도 안 나가고 상대가
바뀌어도 못 알아챈다. 막힌 경로에서 24초까지 갔다 (`_begin_trace` 931).
같은 이유로 `pick_opponent`의 ICMP 프로브도 `ProbeCache` 1228로 감싸 메인 루프가
매 5초마다 막히지 않게 한다.

---

## 2. 데이터 경로 — 패킷 하나가 화면 숫자가 되기까지

```
NIC → Npcap → BPF(커널에서 필터링) → on_packet 1687
   ├ IP/UDP 아니면            → 버림
   ├ is_valve_relay 278       → _relay 에만 적재 (상대 후보에서 제외)
   ├ is_ignorable 287         → 버림 (사설망·루프백·링크로컬·내 IP·기타 노이즈 대역)
   └ traffic.add(ip, pkt.time, outbound) 641

snapshot(time.time()) 655
   ├ _trim 651 — 2초보다 오래된 시각을 앞에서 버린다 (개수가 아니라 시각으로)
   ├ gaps = 인접 도착 시각의 차   ← 수신만 본다.
   │        송신 간격은 내 게임의 프레임 루프가 정하는 값이라 회선과 무관하다
   └ Flow 568
        ├ sampled        표본 5개 이상인가       (아니면 나머지 전부 0)
        ├ median_gap     중앙값                  (평균 아님 — 이상치 강건)
        ├ jitter         p95 − 중앙값            (표준편차 아님)
        ├ frame_gaps     임계 초과 간격의 '횟수'
        ├ starved_frames 굶은 프레임 '분량'      ← 판 등급은 이걸로 낸다
        ├ worst_gap      최댓값
        └ frame_paced    중앙값이 8.3~33.3ms인가

format_osd 1295 / format_log 1325  ←  Flow + Pinger.snapshot() 를 합쳐 한 줄로
   → OSD.show 1168 (RTSS 공유 메모리, ASCII 전용)  /  print + fit_width 322
```

### 이 경로에서 짚고 갈 설계 결정 세 가지

**`0`이 값인가 미측정인가** (`Flow.sampled` 582). 표본이 5개 미만이면 통계를
내지 않고 `median_gap`을 `0.0`으로 둔다. 그런데 화면에서 `0.0ms`는 "재보니 0"으로
읽힌다. 그래서 표시하는 쪽은 **반드시 `sampled`를 먼저 보고** `간격 --(표본 3/5)`로
쓴다 (`format_log` 1351).

**임계값의 바닥** (596). 갭 임계는 `중앙값 × 2`인데, 다운로드처럼 패킷이 뭉쳐
오는 스트림은 중앙값이 0에 가까워져 임계도 0이 된다 — 모든 간격이 갭이 되어버린다.
`max(중앙값 × 2, FRAME_MS)`로 바닥을 둔다. 한 프레임 안에 도착했으면 프레임이
굶은 게 아니다.

**횟수가 아니라 분량** (602). 30ms 갭과 490ms 갭을 똑같이 1회로 세면 안 된다.
앞은 체감이 없고 뒤는 눈에 보이는 멈춤이다. `starved_frames`는 간격이 한 프레임을
넘은 만큼을 더한다 = 게임이 실제로 기다린 프레임 수. 실측 판을 횟수로 재면 D,
분량으로 재면 A가 나왔다 (근거는 FINDINGS `### 갭은 횟수가 아니라...`).

---

## 3. 상대는 어떻게 정해지는가

가장 재구성하기 어려운 경로다. 다섯 함수에 흩어져 있고 5초마다 한 번 돈다.

```
find_processes 188            SoulcaliburVI.exe + steam.exe
  → udp_ports_of 202          psutil 스냅샷 — 요동친다 (실측 3→7→9→19→3→47→58)
  → PortSet.update 241        합집합으로 누적. 빈 목록이 와도 버리지 않는다
  → build_filter 257          포트를 한 번도 못 읽었으면 FALLBACK_BPF (443/53/123/80 제외)
  → BPF 문자열이 바뀔 때만 스니퍼 재시작 1728

traffic.snapshot() → flows
  → pick_opponent 1267
       ├ 현재 상대가 KEEP_RATE(25pkt/s) 이상이면 즉시 유지 ────┐
       ├ 후보 필터  rate ≥ MIN_RATE(50)  AND  two_way 1257    │ 히스테리시스
       ├ 패킷 수 상위 3개를 ProbeCache 1228로 찔러봄 (TTL 60초)│
       └ 아무도 응답 안 하면 트래픽 1위라도 반환 ──────────────┘
  → 상대가 바뀌었을 때만 pinger.set_target 875 + geo.lookup
```

### `PortSet`이 누적하는 이유 (220)

`psutil`이 주는 포트 목록은 그 순간의 스냅샷이라 매번 다르다. 그때마다 BPF를
새로 만들면 **스니퍼를 껐다 켜는 순간의 패킷을 통째로 놓친다.** 16.6ms 간격을
재는 도구가 몇백 ms를 눈감으면 지터·갭이 실제보다 나쁘게 나온다. 그래서 한 번
본 포트는 계속 지켜보고, 목록이 늘기만 하므로 재시작은 '새 포트가 처음 보일
때'로 한정된다. 상한(200)을 넘으면 LRU로 버리되 **몇 개를 버렸는지 로그에 남긴다**
(`dropped` 239 → 1737) — 조용히 자르지 않는다.

### 네 개의 문턱은 서로 다른 질문이다

섞어 읽으면 안 된다.

| 상수 | 값 | 묻는 것 | 어디서 |
|---|---|---|---|
| `MIN_RATE` 73 | 50pkt/s | 새 상대로 **잡을** 만한가 (엄격) | `pick_opponent` 1283 |
| `KEEP_RATE` 77 | 25pkt/s | 이미 잡은 상대를 **놓을** 것인가 (느슨) | `pick_opponent` 1279 |
| `MIN_DOWN_RATIO` 93 | ↓ ≥ 0.25×↑ | 이 IP가 **상대로 볼 만한가** | `two_way` 1257 |
| `MATCH_START_SYMMETRY` 136 | ↓ ≥ 0.8×↑ | 대전이 **실제로 시작됐는가** | `can_start` 1502 |

앞의 두 개가 히스테리시스다. 잡을 때 엄격 / 놓을 때 느슨. 132pkt/s가 순간
흔들릴 때마다 상대를 놓으면 EMA·핑 이력·tracert 이력이 통째로 리셋된다
(실측 렉 구간에서 132 → 16pkt/s까지 떨어졌다).

뒤의 두 개는 이름이 비슷하지만 다른 층이다. `MIN_DOWN_RATIO`는 매칭 잡담
(↑72↓7)을 거르되 **연결 수립 단계(↑66↓30)는 통과시켜야** 한다 — 거기서 상대를
잡아야 ICMP 막힌 상대의 tracert 14초를 미리 시작할 수 있기 때문이다.
`MATCH_START_SYMMETRY`는 그보다 엄격해서, 판 통계는 ↑↓가 대칭이 된 뒤부터 모은다.

또 하나: 대전 중에는 `pick_opponent`가 **첫 줄에서 곧장 빠져나간다.** 그래서
ICMP 프로브가 아예 안 돌고 메인 스레드가 막히지 않는다 (1277~1280).

---

## 4. 능동 측정과 폴백

`Pinger`가 이 파일에서 상태가 가장 많다. `_hop`, `_traced`, `_tracing`,
`_trace_done`, `_trace_result`, `_trace_cache` — 왜 이렇게 쪼개졌는지가 핵심이다.

```
상대 확정 → set_target 875     avg · _history · _hop · _traced 전부 리셋
    └ 다음 사이클에 즉시 _begin_trace 931
         ├ _trace_cache 히트 → 그 자리에서 결과 사용 (tracert 재실행 안 함)
         └ 미스 → 별도 스레드 _trace_worker 952
         · 상대가 ICMP에 응답하면 trace_last_hop()이 None을 주고 결과는 안 쓰인다
         · 미리 시작하는 이유: 예전엔 3초(실패 대기) + 15초(tracert) = 19초 공백

매초  ping_once 705            _hop이 잡혔으면 hop.ip를 대신 찌른다 (909)
    ├ 성공 → _fails = 0,  avg = EMA(α=0.3, 약 2초 수렴)
    └ 실패 → _fails += 1
    → _history.append((시각, rtt|None))     최근 10초 → lo/hi/loss (1021)

_record_trace 967         결과를 '보관만' 한다          ← 도착 시점
_use_trace_if_needed 981  _fails ≥ 3 일 때만 적용       ← 사용 시점
    ├ found is None       → 안내: "응답하는 홉도 없음"
    ├ found.weak 770      → 안내: "경로가 끊겨 추정 불가"  (숫자를 만들지 않는다)
    └ 그 외               → _hop 채택. 이후 값은 전부 '하한' (화면에 >= 표시)
```

### 도착 시점과 사용 시점을 분리한 것

이 클래스의 핵심 아이디어다. 추적을 상대 선정 즉시 시작하므로, 결과가 **"핑이
막혔다"는 판정보다 먼저 도착할 수 있다.** 그때 바로 적용하면 ICMP에 멀쩡히
응답하는 상대까지 홉 RTT(하한)로 바꿔 표시하게 된다. 그래서 받아만 두고
(`_record_trace`), 쓸지는 `_fails`를 보고 따로 정한다 (`_use_trace_if_needed`).

### 폴백 중에는 손실률을 숨긴다

`format_osd` 1320 / `format_log` 1369가 `ping["hop"]`이 있으면 손실을 안 찍는다.
그 손실은 상대가 아니라 **경로 중간 라우터의 ICMP rate-limit**이다. 실측: 11홉
라우터가 10~33%를 흘리는 동안 대전 연결은 132pkt/s ↑66↓66으로 멀쩡했다.

### 안내 문구는 한 번만

`Pinger.trace_note`와 `GeoDB`의 안내는 메인이 `take_note()`로 꺼내 가면 비워진다
(1015 / 475 → 호출부 1760~1765). 2초마다 같은 줄을 도배하지 않기 위한 것.

---

## 5. 판 상태기계

`MatchTracker` 1468. 로그 틱(2초)마다 `feed()` 1510 한 번씩만 불린다.

```
   ┌─ 대기 (self.ip is None)
   │
   │  ok = is_match 1492      frame_paced  AND  rate >= KEEP_RATE
   │  ok AND can_start 1502   ↓ >= 0.8×↑ (연결 수립 중이 아님)
   ▼
   ┌─ 진행 ──────────────────────────────────────────────────────┐
   │  ok 인 창마다 _collect 1544                                  │
   │     rtt / interval / jitter / worst / rate / gaps / starved  │
   │  !ok 인 창에서는 아무것도 안 모은다                          │
   │     ← 붕괴 구간의 지터(실측 338~470ms)가 통계에 안 섞인다     │
   └──────────────────────────────────────────────────────────────┘
   │
   ├ 상대가 바뀜         → 즉시 종료 (유예 없음. 확정적 경계다) 1516
   ├ !ok 가 12초 지속    → 종료 (MATCH_END_GRACE 127, 실측 6초 렉의 2배)
   └ 게임 종료 / Ctrl+C  → flush 1533  (finally 1812를 지나므로 안 버려진다)
   ▼
   _finish 1558
     ├ span < 30초 (MATCH_MIN_SECONDS)  → 요약 없음. 매칭 중 스파이크는 판이 아니다
     └ MatchSummary 1420 → format_match_summary 1571 → 콘솔
                         → SessionTally 1612 (세션 종료 시 롤업)
```

### 시작·유지·종료 조건이 다 다르다

- **시작**은 가장 엄격하다 (`ok` + 대칭 0.8). 연결 수립 중에는 송신이 먼저 붙고
  수신이 뒤따르는데(96pkt/s ↑66↓30 → 132pkt/s ↑66↓66), 그 구간은 갭이 몰려 있어
  그냥 두면 멀쩡한 판의 등급을 통째로 끌어내린다.
- **유지**는 `ok`만 본다. 판 도중의 비대칭은 렉일 수 있고, 그게 종료인지 렉인지
  가를 근거가 아직 없다.
- **종료**는 12초 유예를 둔다. 실측된 렉이 6초짜리였다.

### 등급 — 근거의 강도가 항목마다 다르다

`worst_grade` 1405가 **셋 중 최악**을 종합으로 낸다. 평균을 내면 갭이 터진 판이
"지터는 좋았으니까"로 희석되는데, 갭은 게임이 실제로 멈춘 양이라 다른 지표로
상쇄될 성질이 아니다.

| 지표 | 표 (A / B / C) | 근거의 강도 |
|---|---|---|
| 갭 (`starve_pct`) | 1% / 3% / 10% | **정의상 확실.** 프레임이 굶었다 = 게임이 멈춰 기다렸다 |
| 핑 (편도 프레임) | 1 / 2 / 3 | 격겜 통념에 기댄 값. 중간 |
| 지터 | 2 / 10 / 30ms | **추측이다.** 실측이 정상(0.4~2ms)과 렉(338~470ms) 두 극단뿐 |

세 번째 줄을 그대로 남겨둔 것 자체가 이 프로젝트의 규율이다 — 실측 / 통념 /
추측을 같은 칸에 적지 않는다 (1378~1395).

---

## 6. 생애주기

```
__main__ 1853
  └ match_console_encoding 347   ← run()보다 먼저. 기동 실패 메시지도 안 깨지게
  └ run() 1827
       ├ get_local_ip 266
       ├ GeoDB.load_async 467    ← 데몬 스레드. 없거나 실패하면 조용히 비활성
       ├ Pinger.start()          ← 세션을 넘어 살아남는 유일한 스레드
       └ while True:
            ├ wait_for_game 1643        게임이 뜰 때까지 1초 폴링
            │    (sleep은 POLL_INTERVAL 0.2초로 쪼갠다 = Ctrl+C 응답 시간)
            └ monitor_session 1664      게임이 꺼지면 반환 → 다시 대기
```

`monitor_session`의 `finally` 1812가 되돌리는 것:

| 되돌리는 것 | 왜 |
|---|---|
| `stop_sniffer` | 캡처 스레드 종료 |
| `pinger.set_target(None)` | 다음 세션까지 헛되이 핑을 쏘지 않게 |
| `osd.clear()` | RTSS `dwBusy` 비트를 놔야 오버레이가 안 굳는다 |
| `tracker.flush()` | **Ctrl+C로 끝내도 진행 중이던 판이 안 버려진다** |
| `tally.lines()` | 세션 롤업 출력 |

`OSD`는 세션마다 새로 만든다 — 모니터를 먼저 켜둔 뒤에 RTSS가 떴을 수도 있어서다
(1669).

기동 실패는 `__main__`이 받아 Npcap 안내로 바꾼다 (1862~1871). scapy는 Npcap이
없을 때 `PermissionError`가 아닌 예외를 던지므로 세 종류를 다 받는다.

---

## 7. 본 흐름 밖 — 나중에 읽어도 되는 것

전부 **graceful degradation**이라 본체 흐름과 독립이다. 없으면 그 기능만 빠지고
나머지는 그대로 돈다. 그 코드를 건드릴 때 읽으면 된다.

| 무엇 | 위치 | 없으면 |
|---|---|---|
| `GeoDB` — IP → 국가/ISP | 431 | 국가/ISP 표시만 빠진다 (`available` 536) |
| `OSD` — RTSS 오버레이 | 1040 | 콘솔만 쓴다 (`available` 1165) |
| 콘솔 인코딩 / 줄 폭 | 347 / 311 / 322 | (이건 항상 필요하다) |

곁가지지만 각각 한 가지씩 배울 게 있다.

- **`GeoDB`** — 40만 대역을 `array("I")` + `bisect`로 조회한다 (549). 같은 ASN의
  조직명을 dict로 접어 중복 제거한다 (456~460). "조회했는데 없음"도 캐시해야 해서
  `None`을 센티널로 못 쓴다 → `get(ip, False)` + `is not False` (542).
- **`OSD`** — RTSS는 DLL export가 아니라 **공유 메모리**다. `OpenFileMapping`으로
  '열기'만 해야 한다 — `mmap`으로 만들면 RTSS가 꺼져 있을 때 빈 매핑을 새로 만들어
  허공에 쓰게 된다 (`_attach` 1081). `szOSD`가 char 배열이라 오버레이 문구는
  영문만 쓴다 (1788).
- **인코딩/폭** — Windows 콘솔은 직접 출력이면 유니코드가 그대로 가지만 **파이프를
  물리면 바이트로 나간다.** 그래서 `| Tee-Object` 하는 순간 깨졌다 (347).
  그리고 한글은 1글자가 2칸이라 `len()`이 아니라 `display_width()`로 재서 잘라야
  한다 (311 / 322).

---

## 한 장 요약

```
게임 프로세스 → UDP 포트 → BPF → 패킷 캡처 ─┬→ 도착 간격 → Flow      ─┐
                                             │                         ├→ 화면 (250ms)
                            상대 IP 추정 ────┴→ ICMP 핑 → Pinger.avg  ─┤
                                                                       ├→ 로그 (2초)
                                                                       └→ 판 요약 (판 종료 시)
```

측정은 빠르게, 화면은 천천히. **평균 핑이 아니라 '도착 간격이 튀는 순간'이
체감을 좌우한다** — 딜레이 기반 넷코드는 늦게 온 패킷을 되감아 보정하지 못하고
그냥 멈춰 기다리기 때문이다. 이 도구의 존재 이유 전체가 그 한 문장에 걸려 있다.

> 상대 IP는 개인정보다. 저장하거나 외부에 공유하지 말 것.
