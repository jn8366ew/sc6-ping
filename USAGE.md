# 실행과 출력 읽는 법

이 문서는 도구를 **돌리고 읽는 법**만 다룬다.

| 문서 | 답하는 질문 |
|---|---|
| [README.md](README.md) | 이게 뭔가 |
| [NETWORK.md](NETWORK.md) | 왜 이런 게 문제인가 — 네트워크 개념 |
| [ARCHITECTURE.md](ARCHITECTURE.md) | 어떻게 도는가 — 실행 흐름 |
| [FINDINGS.md](FINDINGS.md) | 왜 그 값인가 — 상수의 실측 근거 |
| **USAGE.md** (여기) | **어떻게 쓰나** — 실행, 출력 필드, 트러블슈팅 |

> 예시 로그의 상대 IP는 뒤 두 옥텟을 가렸다(`14.47.x.x`). 상대 IP는 개인정보다.

---

## 실행

**스크립트를 먼저 켜고 게임을 나중에 실행한다.**

```powershell
cd sc6-ping
uv run monitor.py
```

게임이 뜰 때까지 대기하다가 자동으로 붙는다. 게임을 꺼도 대기 상태로 돌아가므로 여러 판 연속으로 써도 된다. 종료는 `Ctrl+C`.

로그는 따로 뭘 붙이지 않아도 **저장소 폴더에 자동으로 쌓인다.** 켠 순간의
월일-시분으로 이름이 굳는다 — `run-0829-1630.log`. 실행하면 첫 줄에 파일명이 뜬다.

```
[*] 로그: run-0829-1630.log
```

같은 분 안에 다시 켜면 그 파일 뒤에 이어 붙고, 화면에 나가는 것과 표준 에러가
같이 들어간다. 줄 단위로 흘려보내므로 `Ctrl+C`로 끊어도 마지막 줄까지 남는다.

`Tee-Object` 파이프는 이제 필요 없다. 파이프를 물리지 않으면 표준 출력이 콘솔로
남아서, 콘솔 코드페이지(cp949)에 없는 글자 때문에 로그가 깨지던 문제
([FINDINGS 6번](FINDINGS.md#6-파이프로-넘기면-한글이-깨짐--검증됨))가 아예 생기지
않는다 — 화면은 콘솔이 쓰던 방식 그대로, 파일은 UTF-8로 받는다.

로그에는 상대 IP가 남으므로 `run*.log`를 `.gitignore`에 넣어뒀다.

### 사전 준비

| 항목 | 상태 | 비고 |
|---|---|---|
| Npcap | 설치됨 | `Restrict to Administrators only`를 **해제**하고 설치해서 관리자 권한 없이 동작 |
| RTSS | 설치됨 | 오버레이용. 트레이에 떠 있어야 하고, **게임보다 먼저** 실행돼야 후킹이 걸린다 |
| uv | 0.9.21 | `uv sync` 한 번이면 준비 끝 |
| ip2asn-v4.tsv | 선택 | 상대 국가/ISP 표시용. 없으면 그 줄만 빠지고 나머지는 그대로 동작한다 |

RTSS 설정에서 `Start with Windows`, `Show On-Screen Display`를 켜두면 이후 신경 쓸 필요 없다.
실행 순서: **RTSS → 게임 → monitor.py**

상대 국가/ISP를 보려면 대역표를 한 번 받아 저장소 폴더에 둔다. 계정이 필요 없고
받아두면 완전히 오프라인으로 동작한다 — **상대 IP가 밖으로 나가지 않는다.**

```powershell
curl.exe -L -o ip2asn-v4.tsv.gz https://iptoasn.com/data/ip2asn-v4.tsv.gz
# 압축 해제 후 ip2asn-v4.tsv 로 저장소 폴더에 둔다
```

기동할 때 `[*] 국가/ISP 표: ...` 한 줄로 상태를 알려준다. 파일이 없으면
`없음 (국가/ISP 표시 안 함)`이 뜨고 나머지 기능은 그대로다.

---

## 출력 읽는 법

```
22:24:43  115.143.x.x   48.0ms → 2.9프레임  간격 16.6ms  지터 0.2  갭 0.0/s(최대 17)  132pkt/s ↑66↓66
```

한 줄이다. **간격·지터·갭의 단위는 전부 ms**이고, 반복을 피해 첫 필드에만 붙인다.

| 항목 | 뜻 |
|---|---|
| `48.0ms` | ICMP 왕복 시간 |
| `2.9프레임` | 왕복 시간 ÷ 16.67ms. **왕복 기준**이며 게임이 실제로 넣는 입력 딜레이는 대략 이 절반(편도) |
| `간격` | 상대 패킷 도착 간격의 중앙값. 정상 대전이면 **16.6ms** |
| `지터` | 도착 간격의 p95 − p50. 표준편차 대신 백분위를 써서 스파이크 하나에 값이 흔들리지 않게 함 |
| `갭` | 간격이 2프레임을 넘은 횟수/초, 괄호 안은 최악의 간격. **딜레이 기반에서 게임이 멈춰 기다린 횟수** |
| `132pkt/s ↑66↓66` | 초당 패킷 수와 방향별 내역 |
| `(비페이싱)` | 프레임당 1패킷 구조가 아니라 지터·갭이 성립하지 않는 스트림 |
| `간격 --(표본 3/5)` | 통계를 낼 만큼 안 모였다는 뜻. **잰 값이 0인 게 아니다** |
| `>=` 접두사 | TTL 폴백으로 잰 하한값. 실제는 이보다 크다 |
| `핑 무응답` | ICMP가 막힌 것. 사유와 폴백 홉은 `[*]` 안내로 한 번만 나온다 |
| `손실 %` | 핑 손실률. **`>=`가 붙어 있을 때는 아예 안 나온다** — 그 손실은 상대가 아니라 중간 라우터의 ICMP rate-limit이라 게임 손실로 오해된다 |

상대가 ICMP를 막으면 `핑 무응답`이 먼저 나오고, tracert가 폴백 홉을 찾은 뒤
(약 14초) `>=`가 붙은 값으로 바뀐다. 그 사이 오버레이에는 `SC6  ping --`이 뜬다.

콘솔 줄은 터미널 폭을 넘지 않게 잘린다(잘리면 끝에 `…`). 넘긴 줄이 다음 출력과
겹쳐 글자가 섞이는 걸 막기 위한 것이다.

SC6는 **딜레이 기반 넷코드**라 늦게 온 패킷을 되감아 보정하지 못하고 그냥 기다린다.
그래서 평균 핑보다 **갭이 튀는 순간**이 체감을 좌우한다. 평균 핑이 낮아도 갭이 있으면 버벅인다.

### `[*]` 안내 줄

2초마다 나오는 측정 줄과 달리, 상태가 바뀔 때 한 번씩만 나온다.

| 줄 | 언제 |
|---|---|
| `[*] 게임 감지: SoulcaliburVI.exe (PID ...)` | 게임 프로세스를 찾았을 때 |
| `[*] RTSS 오버레이: ...` | 오버레이 연결 상태(미설치·미실행이면 그 사유) |
| `[*] 로컬 IP: 192.168.x.x` | 기동 시 1회. **사설 대역이 아니면 VPN 전체 터널을 의심한다** |
| `[*] 국가/ISP 표: ...` | ip2asn 로딩 결과 |
| `[*] 상대: 14.47.x.x  한국 · Korea Telecom (AS4766)` | 상대를 확정했을 때 |
| `릴레이 경유 매치 ― 상대 IP 노출 안 됨` | 홀 펀칭 실패로 밸브 릴레이만 보일 때. **이 판은 측정 불가** |
| `[*] 이번 세션 N판 (상대 M명)` | 종료 시 세션 총평 |

### 오버레이(RTSS)

```
SC6  KR  4ms  0.3f  JIT 1.0  GAP 0.0
```

국가코드 두 글자 + 핑 + 프레임 환산 + 지터 + 갭. 표본이 모자라면 `JIT`/`GAP`은
아예 안 뜬다(가짜 0을 내보내지 않는다). `szOSD`가 ASCII 전용이라 한글은 못 넣는다.

---

## 판 요약

판이 끝나면 그 판 전체를 한 번에 정리한다.

```
[판 종료] 14.47.x.x  한국 · Korea Telecom (AS4766)   3분 18초
  평가  A   (핑 A / 지터 A / 갭 A)
  핑    4.4ms → 0.3프레임 왕복 (편도 0.1)
  간격  16.3ms
  지터  중앙 1.0ms   최대 98.0ms
  갭    18회   최악 128ms   (5.5회/분)
  굶음  42프레임 / 11,880   (0.35%)
```

게임을 끄거나 `Ctrl+C`로 나가면 그 세션의 판들을 한 번 더 묶어 보여준다.

- **굶음**이 등급의 기준이다. 갭 *횟수*가 아니다 — [왜 바꿨는지](FINDINGS.md#갭은-횟수가-아니라-굶은-프레임으로-잰다)
- 종합 등급은 **셋 중 최악**. 평균을 내면 게임이 멈춘 판이 희석된다
- 핑이 `>=`(폴백 하한)이면 실제 등급은 표시된 것보다 **나쁠 수는 있어도 좋을 수는 없다**
- 30초 미만은 요약을 내지 않는다(`MATCH_MIN_SECONDS`)

임계값과 그 근거의 강도는 [FINDINGS.md](FINDINGS.md#등급-임계값--근거의-강도가-다르다)에 정리돼 있다.

---

## 잘 안 될 때

| 증상 | 원인 / 조치 |
|---|---|
| 오버레이가 게임 화면에 안 뜬다 | RTSS가 **게임보다 먼저** 실행돼야 후킹이 걸린다. RTSS 재시작 후 게임 재실행 |
| `대전 트래픽 없음`만 계속 나온다 | 아직 대전이 아니다. 매칭 잡담은 4~18pkt/s라 상대로 잡지 않는다(실제 대전은 132pkt/s) |
| `핑 무응답`이 계속 뜬다 | 상대가 ICMP를 막은 것. 정상이다 — 14초 뒤 `>=` 하한값으로 바뀐다. [실측 38판 중 36판이 이랬다](NETWORK.md#9-재검증--2026-08-29-13판) |
| 파이프로 넘기니 한글이 깨진다 | 해결된 문제다. 안 고쳐졌다면 콘솔 코드페이지 확인 → [FINDINGS 6번](FINDINGS.md#6-파이프로-넘기면-한글이-깨짐--검증됨) |
| `간격 0.0ms`가 뜬다 | 지금은 `간격 --(표본 3/5)`로 나와야 한다. 0은 '측정 안 함'이지 '재보니 0'이 아니다 |
| 국가/ISP가 안 나온다 | `ip2asn-v4.tsv`가 저장소 폴더에 없다. 없어도 나머지는 정상 동작한다 |

> 상대 IP는 개인정보다. `run.log`를 공유하지 말 것.
