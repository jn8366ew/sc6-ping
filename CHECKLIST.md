# 실전 검증 체크리스트

2026-08-23에 두 판 돌려서 대부분 확인했다. 아래는 **아직 못 본 것**만 남긴 것이다.
확인된 내용과 그 근거는 `README.md`의 "반영되고 실전에서 검증된 변경",
"검증 완료"에 있다.

---

## 0. 시작 전

- [ ] **RTSS가 트레이에 떠 있는가** — 오버레이를 보려면 필수. 게임보다 먼저 떠 있어야 후킹이 걸린다
- [ ] 실행 순서: **RTSS → 게임 → monitor.py** (monitor는 게임이 이미 떠 있어도 붙으니 덜 민감하다)

```powershell
cd C:\Users\FAMILY\projs\sc6-ping
uv run monitor.py 2>&1 | Tee-Object -FilePath run.log
```

종료는 `Ctrl+C`. 관리자 권한 필요 없음.

> `run.log`에는 상대 IP가 남는다. 개인정보이니 커밋하거나 공유하지 말 것.
> (`.gitignore`에 넣어뒀으므로 실수로 커밋되지는 않는다.)

---

## 1. 매칭 단계 — 제일 큰 구멍

두 판 모두 **이미 대전이 진행 중일 때** 붙어서 매칭 화면을 못 봤다.
다음엔 매칭을 걸기 전에 모니터를 먼저 켜둘 것.

- [ ] 매칭 중에 `대전 트래픽 없음`만 반복되는가 (잡담 IP가 상대로 안 잡히는가)
- [ ] `[*] 감시 대상 갱신` 줄이 초반 몇 번 나오다 멈추는가
      (계속 반복되면 `PortSet` 누적이 제대로 안 되는 것)
- [ ] 세션 초반에 `간격 --(표본 3/5)` 같은 게 잠깐 보이는가.
      **`간격 0.0ms`가 보이면 실패** (예전 버그)
- [ ] **50pkt/s 넘는 비대전 IP가 상대로 잡히는가** — 기동 스모크에서 한 번 있었다
      (`78pkt/s`, 간격 161.9ms, ↑71↓7). 재현되면 그 줄을 그대로 남겨둘 것.
      → `pick_opponent()`에 `frame_paced`나 ↑↓ 대칭을 넣을지가 여기서 갈린다

---

## 2. ICMP에 응답하는 상대

지금까지 만난 상대는 전부 ICMP를 막았다. 응답하는 상대를 만나면 확인할 것:

- [ ] `>=` 없이 그냥 `48.0ms → 2.9프레임`으로 나오는가
- [ ] `손실 %`가 정상적으로 표시되는가 (폴백일 때만 숨기는 게 맞는지)
- [ ] `[*] ICMP 차단` 안내가 **안 나오는가**
      — 상대를 잡자마자 tracert를 한 번 돌리긴 하지만 결과를 쓰면 안 된다

---

## 3. 게임 껐다 켜기 / 종료

- [ ] `대기 → 감지 → 세션 → 대기`로 다시 도는가
- [ ] `Ctrl+C`가 1초 안에 먹는가
- [ ] 잔류 프로세스 없는지:

```powershell
tasklist /FI "IMAGENAME eq ping.exe"
tasklist /FI "IMAGENAME eq TRACERT.EXE"
```

---

## 이미 확인된 것 (다시 안 봐도 됨)

- 상대 IP가 판 끝까지 고정 (`106.5.205.155`, 3분)
- 간격 16.6ms, ↑↓ 1:1 (`132pkt/s ↑66↓66`)
- 2초 로그 타임스탬프가 안 밀림 — 렉 구간에서도, tracert 도는 14초 동안에도
- 줄이 안 깨짐 (폭)
- 파이프로 넘겨도 한글이 안 깨짐 (인코딩)
- 오버레이가 게임 화면에 실제로 뜸
- ICMP 막힌 상대에 TTL 폴백이 동작 (`>=55.0ms`, `12홉까지만 추적`)
- `KEEP_RATE` 히스테리시스 — 16pkt/s로 떨어져도 상대를 안 놓음

---

## 끝나고

`run.log`를 보고 위 항목을 채운다. 새로 드러난 게 있으면 README의
"알려진 문제 / 다음 작업"에 추가한다.
