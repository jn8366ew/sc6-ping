# 실전 검증 체크리스트

한 판 돌리면서 확인할 것. 코드는 1~5번 전부 반영됐고 합성 테스트는 통과했지만,
**실제 대전에서 확인한 건 아직 하나도 없다.**

---

## 0. 시작 전

- [ ] **RTSS가 트레이에 떠 있는가** — 오버레이를 보려면 필수. 게임보다 먼저 떠 있어야 후킹이 걸린다
- [ ] 실행 순서: **RTSS → 게임 → monitor.py** (monitor는 게임이 이미 떠 있어도 붙으니 덜 민감하다)

```powershell
cd C:\Users\FAMILY\projs\sc6-ping
uv run monitor.py 2>&1 | Tee-Object -FilePath run.log
```

종료는 `Ctrl+C`. 관리자 권한 필요 없음.

> `run.log`에는 상대 IP가 남는다. 개인정보이니 커밋하거나 공유하지 말 것.
> (`.gitignore`에 넣어뒀으므로 실수로 커밋되지는 않는다.)

---

## 1. 기동 직후

이런 줄이 나와야 한다:

```
[*] 로컬 IP: 192.168.0.61
[*] 화면 4Hz / 로그 2초  (Ctrl+C 로 종료)

[*] 게임 감지: SoulcaliburVI.exe (PID 45664)
[*] RTSS 오버레이: 연결됨 (공유 메모리 v2.21)

[*] 감시 대상 갱신: 40개 포트(누적)
```

- [ ] `RTSS 오버레이:` 가 **`연결됨`** 인가
  - `RTSS 미실행` → 트레이 확인하고 RTSS 켜고 다시 시작
- [ ] `감시 대상 갱신` 에 **`(누적)`** 이 붙어 있는가 ← 4번이 들어간 표시

---

## 2. 매칭 중

- [ ] **잡담 IP가 상대로 안 잡히는가** — `대전 트래픽 없음`만 반복되면 정상
- [ ] `[*] 감시 대상 갱신` 줄이 **초반 몇 번 나오다 멈추는가** ← 4번의 핵심.
      계속 반복되면 포트 누적이 제대로 안 되는 것
- [ ] 세션 초반에 `간격 --(표본 3/5)` 같은 게 잠깐 보이는가 ← 3번.
      **`간격 0.0ms` 가 보이면 실패** (예전 버그)

### ⚠ 이번에 제일 궁금한 것

기동 스모크에서 이런 게 잡혔다:

```
00:49:33  114.98.103.191   핑 무응답  간격 161.9ms(비페이싱)  78pkt/s ↑71↓7
```

**78pkt/s.** README에 적힌 매칭 잡담은 4~18pkt/s인데 이건 `MIN_RATE = 50`을 넘겨
상대로 뽑혔다. 대전이 아닌 건 확실하다 — 간격 161ms에 ↑↓ 비대칭이니까.

- [ ] **매칭 중에 50pkt/s 넘는 IP가 상대로 잡히는가?** 잡힌다면 그 줄을 그대로 남겨둘 것.
      → 선정 조건에 `frame_paced`나 ↑↓ 대칭을 추가할지가 여기서 갈린다

---

## 3. 대전 중 (제일 중요)

정상이면 이런 줄이 2초마다 나온다:

```
22:24:43  115.143.25.127   48.0ms → 2.9프레임  간격 16.6ms  지터 0.2  갭 0.0/s(최대 17)  132pkt/s ↑66↓66
```

- [ ] **상대 IP가 판 끝까지 안 바뀌는가** ← 1번의 핵심. 중간에 다른 IP로 튀면 실패
- [ ] `간격` 이 **16.6ms 근처**, `↑↓` 가 **거의 1:1** 인가 (기존 전제가 안 깨졌는지)
- [ ] **줄이 안 깨지는가** ← 2번. 창을 좁혀서 다시 봐도 글자가 섞이지 않고
      끝에 `…` 가 붙으며 잘려야 정상
- [ ] **타임스탬프 간격이 일정한가** — 2초씩 규칙적이어야 한다.
      3~4초씩 밀리면 메인 스레드가 어딘가에서 막히는 것

### 핑이 무응답일 때 (ICMP 막힌 상대)

- [ ] `[*] ...: ICMP 차단 ...` 안내가 **판당 최대 한 번**만 뜨는가 ← 5번.
      여러 번 반복되면 캐시가 안 먹은 것
- [ ] 그 안내가 뜨는 **동안에도 아래 로그 줄이 계속 갱신되는가** ← 5번의 핵심.
      예전엔 tracert 도는 24초간 통째로 멈췄다
- [ ] 안내에 `12홉까지만 추적` 이 붙었다면 정상 (하한을 경로 중간에서 쟀다는 뜻)

---

## 4. 오버레이 (미확인 항목)

게임 화면을 직접 봐야 한다. 분기 진단:

- [ ] 게임 화면에 **RTSS 자체 FPS 카운터**가 뜨는가?
  - **FPS도 안 뜸** → 후킹 문제. RTSS detection level을 High로 올리거나 SC6 프로필
    수동 추가. **코드와 무관**
  - **FPS는 뜨는데 우리 글자(`SC6 48ms 2.9f ...`)만 없음** → `OSD.show()`의 슬롯 선택
    문제. `RTSS-CLI` 폴백을 검토
  - **둘 다 뜸** → 미확인 항목 해결

---

## 5. 종료 후

- [ ] `Ctrl+C` 가 **바로 먹는가** (1초 안에)
- [ ] 게임을 껐다 켰을 때 `대기 → 감지 → 세션` 으로 다시 도는가
- [ ] 잔류 프로세스 없는지:

```powershell
tasklist /FI "IMAGENAME eq ping.exe"
tasklist /FI "IMAGENAME eq TRACERT.EXE"
```

---

## 끝나고

`run.log` 를 그대로 두면 된다. 확인된 항목은 `README.md` 의
"알려진 문제 / 다음 작업"에서 지우고 "실측으로 확인된 것"으로 옮긴다.

**미해결로 남아 있는 것:** 위 2번의 78pkt/s 건. 실전에서 재현되면
`pick_opponent()` 의 선정 조건을 손봐야 한다.
