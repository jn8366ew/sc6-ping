# 내일 할 것

한 판만 제대로 돌리면 지금 미검증인 것들이 대부분 정리된다.
**매칭을 걸기 전에 모니터를 먼저 켜두는 게 핵심이다** — 지금까지 세 판 모두
이미 대전 중일 때 붙어서 매칭 단계를 한 번도 못 봤다.

---

## 준비 — 한 번만 하면 된다

### 1. 대역표 받기

이걸 받아야 상대 국가/ISP가 뜬다. 없어도 나머지는 다 동작한다.

```powershell
cd C:\Users\FAMILY\projs\sc6-ping
curl.exe -L -o ip2asn-v4.tsv.gz https://iptoasn.com/data/ip2asn-v4.tsv.gz
```

압축을 풀어서 **`ip2asn-v4.tsv`** 라는 이름으로 이 폴더에 둔다.
(반디집으로 풀거나, PowerShell에서 gzip을 풀 수 있는 도구로.)

- [ ] 받아서 압축 풀기
- [ ] 기동했을 때 `[*] 국가/ISP 표: ...개 대역 / ASN ...개` 로 뜨는지
      → `없음`이라고 뜨면 파일 이름이나 위치가 틀린 것

> 형식이 문서와 다를 수 있다. 그러면 `형식을 알 수 없음`이라고 뜨고 그냥
> 비활성화된다. 그 줄을 그대로 붙여주면 파서를 고치겠다.

### 2. RTSS 켜기

- [ ] 트레이에 RTSS가 떠 있는가 — **게임보다 먼저** 떠 있어야 후킹이 걸린다

---

## 실행

```powershell
cd C:\Users\FAMILY\projs\sc6-ping
uv run monitor.py 2>&1 | Tee-Object -FilePath run.log
```

순서는 **RTSS → 게임 → monitor.py**. 종료는 `Ctrl+C` (창을 그냥 닫으면
오버레이 글자가 화면에 얼어붙는다).

> `run.log`에는 상대 IP가 남는다. 개인정보이니 공유하지 말 것.
> `.gitignore`에 넣어놨으니 실수로 커밋되지는 않는다.

---

## ① 기동 직후

- [ ] `RTSS 오버레이: 연결됨` 인가
- [ ] `국가/ISP 표:` 에 대역 개수가 나오는가
- [ ] `감시 대상 갱신: N개 포트(누적)` — `(누적)` 이 붙어 있는가

---

## ② 매칭 거는 중 ← **제일 중요**

여기가 한 번도 못 본 구간이다. 매칭을 걸고 화면을 그대로 두고 본다.

- [ ] **`대전 트래픽 없음` 만 반복되는가**

      어제 이런 게 잡혔었다. 이제 안 잡혀야 정상이다:
      ```
      00:17:27  14.10.134.161  핑 무응답  간격 182.6ms(비페이싱)  79pkt/s ↑72↓7
      ```
      **또 잡히면 그 줄을 그대로 남겨줄 것**

- [ ] `[*] 감시 대상 갱신` 이 초반 몇 번 나오다 **멈추는가**
      (계속 반복되면 포트 누적이 안 되는 것)

- [ ] `간격 --(표본 3/5)` 는 정상. **`간격 0.0ms` 가 보이면 실패**

---

## ③ 대전이 시작될 때

- [ ] **몇 초 안에 상대가 잡히는가** ← 어제 필터를 넣어서 이쪽도 봐야 한다.
      너무 엄격해서 진짜 대전을 놓치면 안 된다

- [ ] `[*] 상대: 14.47.163.10  한국 · Korea Telecom (AS4766)` 같은 줄이 뜨는가

- [ ] 게임 화면 오버레이 맨 앞에 **국가코드 두 글자**가 붙는가
      ```
      SC6  KR  4ms  0.3f  JIT 1.0  GAP 0.0
      ```

---

## ④ 대전 중

- [ ] 상대 IP가 판 끝까지 안 바뀌는가
- [ ] `간격` 이 16.6ms 근처, `↑↓` 가 거의 1:1 인가
- [ ] 2초 로그 간격이 일정한가 (3~4초로 밀리면 어딘가 막힌 것)

**ICMP에 응답하는 상대라면** (`>=` 가 안 붙으면):

- [ ] `48.0ms → 2.9프레임` 처럼 `>=` 없이 나오는가
- [ ] `[*] ICMP 차단` 안내가 **안 나오는가**

---

## ⑤ 판이 끝났을 때

```
[판 종료] 14.47.163.10  한국 · Korea Telecom (AS4766)   3분 18초
  평가  A   (핑 A / 지터 A / 갭 A)
  ...
  굶음  42프레임 / 11,880   (0.35%)
```

- [ ] 요약이 **한 번만** 뜨는가
- [ ] **등급이 납득되는가** ← 어제 지표를 바꾼 뒤 처음 보는 판이다.
      깨끗한 판이 A, 렉이 있었으면 B~C 로 나와야 맞다
- [ ] 램프업이 빠졌는가 — 판 길이가 상대 잡힌 시각이 아니라
      **↑↓ 대칭이 된 시각**부터 재져야 한다

---

## ⑥ 두 판 연속으로 ← **재본 적 없는 값**

한 판 끝나고 바로 재대전을 건다.

- [ ] **두 판이 하나로 안 붙는가**

      판 사이 간격이 **12초보다 짧으면 붙어버린다.** SC6 결과 화면과 재대전
      대기가 그보다 길 거라고 봤지만 실제로 재본 적이 없다.
      붙으면 `MATCH_END_GRACE` 를 줄여야 한다

---

## ⑦ 끝낼 때

- [ ] `Ctrl+C` 가 1초 안에 먹는가
- [ ] 진행 중이던 판이 마감되고 **세션 롤업**이 뜨는가
- [ ] 게임을 껐다 켜면 `대기 → 감지 → 세션` 으로 다시 도는가
- [ ] 잔류 프로세스 없는지:

```powershell
tasklist /FI "IMAGENAME eq ping.exe"
tasklist /FI "IMAGENAME eq TRACERT.EXE"
```

---

## 이런 게 보이면 꼭 남겨줄 것

로그를 통째로 안 줘도 된다. 아래 세 가지만 그 줄과 **앞뒤 두 줄씩** 복사해주면 된다.

### 1. 렉 구간 — 제일 필요하다

지터가 수십~수백 ms로 튀는 구간. **`↑↓` 값이 꼭 필요하다.**

진짜 판 종료는 `↑139 ↓13` 처럼 비대칭인데, 렉일 때도 그런지를 모른다.
알면 판 종료 유예를 12초에서 2~4초로 줄일 수 있다.

### 2. `간격 0.0ms(비페이싱)`

어제 딱 한 번 나왔다.
```
23:59:07  14.47.163.10  4.5ms → 0.3프레임  간격 0.0ms(비페이싱)  130pkt/s ↑65↓64
```
수신 패킷 절반이 같은 시각으로 찍혔다는 뜻이다. 자주 나오면 표시를 손봐야 한다.

### 3. 매칭 중에 상대가 잡히는 줄

`대전 트래픽 없음` 이 아닌 게 매칭 단계에서 나오면.

---

## 이미 확인된 것 (다시 안 봐도 됨)

- 상대 IP가 판 끝까지 고정
- 간격 16.6ms, ↑↓ 1:1
- 2초 로그가 안 밀림 — 렉 구간에서도, tracert 도는 14초 동안에도
- 줄 안 깨짐 (폭), 파이프로 넘겨도 한글 안 깨짐 (인코딩)
- 오버레이가 게임 화면에 실제로 뜸
- ICMP 막힌 상대 → TTL 폴백 동작 / 응답하는 상대 → 국내 4.4ms 정상
- 갭 집계가 로그 손계산과 일치 (25회 = 25회)
- `KEEP_RATE` 히스테리시스 — 16pkt/s로 떨어져도 상대를 안 놓음
- 판 요약이 한 번만 출력, 판 경계 3분 18초 정확
